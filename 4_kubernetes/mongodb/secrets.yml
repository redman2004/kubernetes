# FIXME
# I must contain the mongodb-admin-credentials secret
apiVersion: v1
kind: Secret
metadata:
    name: mongodb-admin-credentials
stringData:
    MONGO_INITDB_ROOT_USERNAME: "sundaland_user"
    MONGO_INITDB_ROOT_PASSWORD: "sundaland_password"
